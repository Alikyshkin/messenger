# Мессенджер

Простой кроссплатформенный мессенджер для **Web**, Android, iOS, Windows и macOS.

## Возможности

- **Регистрация и вход** по имени пользователя и паролю
- **Чаты** — список диалогов с последним сообщением
- **Контакты** — добавление по имени пользователя, удаление
- **Переписка** в реальном времени (WebSocket)
- **Вложения** — прикрепление файлов к сообщениям (до 15 МБ, изображения отображаются в чате)
- **Звонки** — голосовые и видеозвонки по WebRTC
- **Пропущенные звонки** — автоматическое создание системных сообщений о пропущенных звонках в чате
- **Реакции на сообщения** — как в Telegram, работают в личных и групповых чатах
- **E2E шифрование** — сквозное шифрование сообщений с ключами только на устройствах пользователей

---

## Быстрый старт

### Установка и запуск

**Сервер:**
```bash
cd server
cp .env.example .env
# Отредактируйте .env: задайте JWT_SECRET
npm install
npm start
```

**Клиент:**
```bash
cd client
flutter pub get
flutter run
```

Выберите устройство: **Chrome** (веб), Android, iOS, Windows, macOS.

📖 **Подробная инструкция:** [docs/getting-started.md](docs/getting-started.md)

---

## Скачать приложение

Готовые сборки лежат в [**Releases**](/releases). Откройте последний релиз и скачайте файл под свою систему:

| Ваша ОС | Файл | Инструкция |
|---------|------|------------|
| **Android** | `messenger-android-*.apk` | Открыть файл на телефоне и разрешить установку из неизвестных источников |
| **Windows** | `messenger-windows-*.zip` | Распаковать архив и запустить `client.exe` |
| **macOS** | `messenger-macos-*.zip` | Распаковать и запустить приложение (при первом запуске разрешить в настройках) |
| **Linux** | `messenger-linux-*.zip` | Распаковать и запустить приложение из папки |
| **Web** | `messenger-web-*.zip` | Распаковать на веб-сервер или использовать локально |

**Важно:** для работы приложения нужен запущенный сервер. См. [Развёртывание](docs/deployment.md).

---

## Документация

Вся документация находится в папке [`docs/`](docs/):

- 📚 **[Главная страница документации](docs/README.md)** — навигация по всей документации
- 🚀 **[Быстрый старт](docs/getting-started.md)** — установка и первый запуск
- ⚙️ **[Настройка секретов](docs/setup-secrets.md)** — настройка GitHub Actions, Docker Hub, SSH
- 🚢 **[Развёртывание](docs/deployment.md)** — полное руководство по развёртыванию на VPS, Docker, HTTPS
- 💻 **[Руководство разработчика](docs/development.md)** — тестирование, CI/CD, стандарты кода
- 🔧 **[Эксплуатация](docs/operations.md)** — бэкапы, мониторинг, кэширование, политика хранения данных
- 📖 **[Функции](docs/features/)** — push-уведомления, архитектура хранения данных
- 🗄️ **[Миграции БД](docs/migrations.md)** — управление схемой базы данных

---

## Безопасность и приватность

### Где хранятся данные и как они защищены

| Данные | Где хранятся | Защита |
|--------|----------------|--------|
| **Пароли** | Сервер, таблица `users` | Не хранятся в открытом виде: только хеш bcrypt. Подобрать пароль по хешу практически невозможно. |
| **Переписки** | Сервер, таблица `messages` | Текст сообщений шифруется **на устройстве пользователя** (E2EE): ключи хранятся только на клиенте (secure storage). Сервер сохраняет уже зашифрованный текст и не может его прочитать. Алгоритм: X25519 (ECDH) + HKDF + AES-256-GCM. |
| **Ключи E2EE** | Только на устройстве пользователя (клиент) | Приватный ключ — в secure storage; публичный отправляется на сервер для обмена с контактами. Сервер не хранит приватных ключей. |
| **Логины (username)** | Сервер, БД | Хранятся открыто (нужны для входа и поиска). Не пароли. |
| **JWT-токены** | Только на устройстве пользователя (клиент) | Подписываются секретом `JWT_SECRET`. В продакшене задайте длинный случайный `JWT_SECRET` в `.env`. |

**Первый запуск:** скопируйте `server/.env.example` в `server/.env` и задайте в `.env` свои значения:
- `JWT_SECRET` — длинная случайная строка (например сгенерированная парольной программой).
- `ENCRYPTION_KEY` — необязательно; только для расшифровки старых сообщений, если они были зашифрованы на сервере до перехода на E2EE.

**Важно:** по сети всё должно идти по **HTTPS** (и WSS для WebSocket). На хостинге включите SSL (Let's Encrypt) и в клиенте укажите `https://` в `config.dart`.

---

## Архитектура

### Как у других мессенджеров

В проекте реализованы подходы, описанные в обзоре: [docs/features/how-messengers-store-data.md](docs/features/how-messengers-store-data.md).

- **Local-first:** источник правды для экранов — локальная SQLite; данные с сервера подмешиваются при загрузке и по WebSocket.
- **Офлайн:** при отсутствии сети сообщения попадают в очередь и отправляются автоматически при появлении связи.
- **E2E:** ключи шифрования хранятся только на устройствах пользователей (как в Signal/WhatsApp).

### Стек

- **Клиент:** Flutter (Dart), Provider, HTTP, WebSocket, SQLite (локальный кэш), E2EE (cryptography, flutter_secure_storage), connectivity_plus
- **Сервер:** Node.js, Express, SQLite, JWT, WebSocket (ws)

### Структура

```
messenger/
├── server/           # Backend API и WebSocket
│   ├── index.js
│   ├── db.js
│   ├── auth.js
│   ├── realtime.js
│   └── routes/
├── client/            # Flutter приложение
│   └── lib/
│       ├── main.dart
│       ├── config.dart
│       ├── database/   # локальная SQLite (чаты, сообщения, outbox)
│       ├── models/
│       ├── services/
│       └── screens/
└── docs/              # Документация
```

---

## Развёртывание

### Варианты хостинга

1. **VPS (сервер в облаке)**  
   Арендуете виртуальный сервер (например **Hetzner**, **DigitalOcean**, **Selectel**, **Timeweb**), ставите Node.js или Docker, клонируете репозиторий, настраиваете `.env`, запускаете сервер.  
   📖 **Подробно:** [docs/deployment.md](docs/deployment.md)

2. **PaaS (платформа)**  
   **Railway**, **Render**, **Fly.io** — заливаете код, задаёте переменные `JWT_SECRET`, `PORT`.  
   ⚠️ Учтите: у многих PaaS диск эфемерный (при перезапуске данные теряются). Нужен либо встроенный объём (persistent volume), либо внешняя БД.

3. **Свой компьютер**  
   Можно запускать сервер у себя и открыть порт (или использовать ngrok/Cloudflare Tunnel). Данные тогда только на вашем ПК; бэкап — копировать папку `server/` и особенно `messenger.db`.

**Рекомендация для начала:** VPS с Ubuntu, установить Docker, настроить Nginx + Let's Encrypt для HTTPS, в cron раз в день копировать `messenger.db` в бэкап.

📖 **Полное руководство:** [docs/deployment.md](docs/deployment.md)

---

## Тестирование

### Сервер

```bash
cd server
npm test
```

Запускаются тесты API (регистрация, логин, сброс/смена пароля, пользователи, сообщения). Используется **тестовая БД в памяти** (`MESSENGER_DB_PATH=:memory:`), файл `messenger.db` не трогается.

### Клиент

```bash
cd client
flutter test
```

Запускаются юнит-тесты моделей и сервисов. Папка `test/` в релизные сборки (`flutter build apk`, `flutter build web` и т.д.) **не входит** — пользователи получают только код из `lib/`.

📖 **Подробно:** [docs/development.md#тестирование](docs/development.md#тестирование)

---

## Качество кода

Проект использует множество автоматических проверок для обеспечения качества кода:

- ✅ **Pre-commit hooks** — форматирование и анализ кода перед коммитом
- ✅ **CI/CD проверки** — автоматические тесты и проверки при каждом push
- ✅ **Dependabot** — автоматические обновления зависимостей
- ✅ **CodeQL** — анализ кода на уязвимости
- ✅ **Conventional Commits** — стандартизированные сообщения коммитов

📖 **Подробно:** [docs/development.md](docs/development.md)

---

## Версионирование

Проект использует [Semantic Versioning](https://semver.org/):

- **PATCH** (1.0.0 → 1.0.1): исправления багов
- **MINOR** (1.0.0 → 1.1.0): новые функции (обратно совместимые)
- **MAJOR** (1.0.0 → 2.0.0): критические изменения

Версия хранится в `client/lib/config/version.dart`. При изменении функциональности необходимо обновить версию.

📖 **Подробно:** [docs/development.md#версионирование](docs/development.md#версионирование)

---

## Создание релиза

1. Обновите версию в `client/lib/config/version.dart` и `client/pubspec.yaml`
2. Создайте тег:
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```
3. GitHub Actions автоматически создаст релиз и соберёт сборки для всех платформ

---

## Лицензия

[Укажите лицензию проекта]

---

## Поддержка

Если у вас возникли вопросы:
1. Проверьте [документацию](docs/README.md)
2. Изучите [Troubleshooting](docs/deployment.md#troubleshooting)
3. Создайте [Issue](https://github.com/yourusername/messenger/issues)
