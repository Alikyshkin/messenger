# Порт сервера (по умолчанию 3000)
PORT=3000

# Секрет для JWT. Обязательно задайте свой длинный случайный ключ в продакшене.
JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars

# Путь к SQLite-базе (чаты и пользователи). На сервере лучше задать папку data, чтобы база не терялась при деплое.
# MESSENGER_DB_PATH=/opt/messenger/data/messenger.db

# Сброс пароля по email: база ссылки в письме (без слэша в конце)
# APP_BASE_URL=https://your-server.com

# SMTP для отправки писем (если не задано — ссылка сброса выводится в консоль сервера)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM=noreply@example.com

# Опционально: только для расшифровки старых сообщений, зашифрованных на сервере до перехода на E2EE.
# Новые сообщения шифруются на клиенте (ключи у пользователя).
# ENCRYPTION_KEY=your-encryption-key-min-16-chars

# CORS: разрешённые домены (через запятую). По умолчанию разрешены localhost для разработки.
# CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Firebase Cloud Messaging для push-уведомлений (опционально)
# FCM_SERVICE_ACCOUNT_PATH=/path/to/firebase-service-account.json
# Или
# FCM_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# OAuth провайдеры (опционально — кнопки входа отображаются только при настройке)
# Google: создайте OAuth 2.0 Client ID в Google Cloud Console
# GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=xxx

# VK: создайте приложение на vk.com/apps?act=manage
# VK_APP_ID=12345678
# VK_CLIENT_SECRET=xxx

# Telegram: создайте бота через @BotFather
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# Вход по телефону (SMS через Twilio)
# SMS_PROVIDER=twilio
# TWILIO_ACCOUNT_SID=ACxxx
# TWILIO_AUTH_TOKEN=xxx
# TWILIO_FROM_NUMBER=+1234567890
