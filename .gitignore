# Server
server/.env
server/messenger.db
server/node_modules
server/uploads
server/public
data

# Client
client/.dart_tool
client/.packages
client/build
client/*.iml

# CI deploy key (created at runtime, never commit)
deploy_key

# IDE / OS
.idea
.DS_Store
*.log
